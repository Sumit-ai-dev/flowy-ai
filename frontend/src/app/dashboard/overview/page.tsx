import { FlowyWorkspace } from '@/components/flowy/flowy-workspace';

export const metadata = {
  title: 'Flowy | PM Command Center'
};

export default function page() {
  return <FlowyWorkspace />;
}
